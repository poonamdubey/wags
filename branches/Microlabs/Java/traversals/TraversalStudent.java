/**
 * @author: Michael Kepple
 * @date: Mar 16, 2012
 * @file: TraversalStudent.java
 *
 * Description: Student class for Traversal exercise.
 */

public class TraversalStudent
{
    //<end!TopSection>
    /* Your objective in this exercise is to create 3 methods, each which will be
     * passed a binary tree. The three methods will return Strings representing a
     * pre-, in-, and post- order traversal of the binary tree.
     */
    String preorderTraversal(Node tree)
    {
        // Declare local string to append traversal results onto

        // If tree is not null, note current node's key

        // Recurse left

        // Recurse right

        // Return String
        return null;
    }

    String inorderTraversal(Node tree)
    {
        // For inorder traversals:

        // Recurse left

        // Note current node's key

        // Recurse right
        return null;
    }

    String postorderTraversal(Node tree) 
    {
        // For postorder traversals:

        // Recurse left

        // Recurse right

        // Note current node's key
        return null;
    }
    //<end!MidSection>
}
