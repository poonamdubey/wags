/**
 * @author: Michael Kepple
 * @date: Mar 14, 2012
 * @file: PostOrderStudent.java
 *
 * Description: Student class for postOrder exercise.
 */

public class PostOrderStudent
{
    //<end!TopSection>
    /* Your objective in this macro-exercise is to construct a binary search tree
     * given only a passed in postOrder traversal of the tree.
     */
    public Node postOrderBuild(String postorder)
    {
        // if length = 0, tree = null

        // find, create root node (always located at a certain position in
        //   post order traversal

        // if length is 1, return current node (recursive base case).

        // count the number of elements in the left subtree

        // using the number of elements calculated to be in the left/right
        //   subtree, create two seperate strings for left/right subtrees.

        // set currentNode's left Node to recursive call on left subtree
        //   string.

        // set currentNode's right Node to recursive call on right subtree
        //   string.

        // return node.
        return null;
    }
    //<end!MidSection>
}
